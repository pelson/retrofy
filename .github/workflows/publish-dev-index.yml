name: publish-dev-index

# Publish a PEP 503 simple index of the current main-branch dev retrofy
# to GitHub Pages so downstream projects can install bleeding-edge
# retrofy without waiting for a PyPI release:
#
#   pip install --extra-index-url \
#     https://pelson.github.io/retrofy/simple/ retrofy
#
# The index is rebuilt from scratch on every successful push to main —
# no history is kept.

on:
  workflow_run:
    workflows: ["python-test"]
    types: [completed]
    branches: [main]

permissions:
  contents: read
  actions: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publish:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Check out the commit that produced the dist
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Download retrofy dev dist from the triggering python-test run
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist-staging/
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate PEP 503 simple index
        run: |
          uvx simple-repository-generator dist-staging/ \
            --output public/ --copy --force
          ls -R public/ | head -50

      - name: Copy playground into the Pages site
        run: |
          if [ -d docs/playground ]; then
            mkdir -p public/playground
            cp -r docs/playground/. public/playground/
            # The Playwright test harness has no place on a static site.
            rm -rf public/playground/tests
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
